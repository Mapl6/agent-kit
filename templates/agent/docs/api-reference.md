# API reference (optional)

> Prefer `/agent/docs/client-data.md` for day-to-day frontend work. Keep this file
> only if the repo owns HTTP endpoints or you want a human-readable endpoint list.
> Consider generating from OpenAPI instead of hand-maintaining.

## `GET /api/[resource]`
- **Description:** [what it does]
- **Auth:** [required/none]
- **Response:**
```json
{ "example": "response shape" }
```

## `POST /api/[resource]`
- **Description:** [what it does]
- **Body:**
```json
{ "example": "request shape" }
```
- **Response:** `[status code + shape]`

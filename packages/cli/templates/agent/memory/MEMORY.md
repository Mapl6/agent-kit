# MEMORY.md

Curated cross-session **project facts** (not a transcript). Soft budget ~2200 characters.
Separate entries with a line containing only `---`.

Prefer facts agents need every session: stack, ports, non-obvious conventions, lessons.
Skip anything already in `AGENTS.md` / skills / SOUL. Narrative goes in `session-log.md`.

<!-- agent-kit:generated:memory:start -->
[Run `npx @mapl6/agent-kit index` then fill durable facts from the report.]
<!-- agent-kit:generated:memory:end -->
